下面是我们目前达成的评测集设计共识，可以直接放进你的项目文档或 `eval_design.md` 里。

---

# OnboardRAG 评测集设计共识

## 1. 项目评测场景定位

项目定位为：

> **OnboardRAG：面向中国企业新员工的入职与办公支持知识助手。**

它不是泛企业知识库，也不是完整 IT 运维工单系统，而是围绕新员工入职早期高频问题，覆盖：

* 员工手册中的人事制度；
* 统一账号与门户登录；
* VPN / 内网访问；
* 钉钉 / 企业微信 / 飞书 / OA 等办公平台；
* 知识库无法回答时的兜底与转人工。

评测集不追求复现真实企业内部工单，而是基于公开可参考材料和项目知识库文档，构造“新员工在真实入职场景下可能提出的问题”。每个问题都应能追溯到文档、章节或证据片段，避免凭空编造。

---

# 2. 一级意图分类

最终采用 **5 个一级意图**：

```text
人事与员工事务
账号与统一门户
内网访问
办公平台
兜底与转人工
```

第一版不设置严格二级意图。细粒度信息用 `topic_tags` 表示，例如 `["请假", "OA审批", "验证码"]`。这样可以降低意图体系复杂度，避免把项目变成意图识别系统。

---

## 2.1 人事与员工事务

### 覆盖范围

这一类对应员工手册中的人事、考勤、薪酬、福利等内容，不只包括入职流程。

包括：

* 入职；
* 聘用；
* 转正；
* 调动；
* 离职；
* 退休；
* 考勤；
* 休假；
* 薪酬；
* 绩效；
* 员工福利；
* 其他人事制度。

### 典型问题

```text
新员工入职第一天需要完成哪些事项？
试用期转正需要走什么流程？
忘记打卡怎么办？
请假需要提前申请吗？
工资条在哪里查看？
离职需要办理哪些手续？
```

### 和其他意图的区分

如果问题核心是**制度规则本身**，归入“人事与员工事务”。

例如：

> “年假怎么计算？”
> “转正流程是什么？”
> “请假需要提前多久申请？”

这些属于人事规则。

如果问题核心是**在哪个平台提交申请**，则归入“办公平台”。

例如：

> “请假审批在哪里提交？”
> “OA 里怎么发起请假流程？”

这些属于办公平台操作。

---

## 2.2 账号与统一门户

### 覆盖范围

这一类处理统一账号、身份认证和统一门户登录相关问题。

包括：

* 统一账号；
* 账号激活；
* 首次登录；
* 密码修改；
* 密码找回；
* 短信验证码；
* 扫码登录；
* 二次验证；
* 手机号变更；
* 账号锁定；
* 统一门户入口；
* OA / VPN / 移动办公平台是否共用统一账号。

### 典型问题

```text
我刚入职，第一次登录公司系统需要先激活统一账号吗？
忘记统一账号密码怎么办？
收不到短信验证码怎么办？
电脑端登录为什么要用企业微信扫码？
OA 能登录，但是 VPN 提示账号密码错误，是不是账号不一样？
```

### 和其他意图的区分

如果问题核心是**账号、密码、验证码、登录认证**，归入“账号与统一门户”。

如果问题核心是**登录某个具体平台后的功能怎么用**，归入对应业务意图。

例如：

> “统一账号密码忘了怎么办？” → 账号与统一门户
> “OA 里怎么提交报销？” → 办公平台
> “VPN 登录后打不开内网系统怎么办？” → 内网访问

---

## 2.3 内网访问

### 覆盖范围

这一类不直接命名为“VPN与内网访问”，而是更抽象地叫 **内网访问**。因为 VPN 只是访问内网的一种方式，用户真正关心的是能不能访问内部系统。

包括：

* VPN 安装；
* VPN 登录；
* VPN 连接失败；
* 内网系统访问；
* 远程办公访问；
* 在家访问 OA / 内部系统；
* VPN 登录成功但打不开系统；
* 某些资源未开放 VPN 访问；
* 内网访问前置条件；
* 常见访问异常。

### 典型问题

```text
在家访问 OA 系统需要先连 VPN 吗？
VPN 登录成功了，但内网系统还是打不开怎么办？
VPN 提示账号密码错误应该先检查什么？
为什么连上 VPN 后有些系统还是访问不了？
外网可以直接访问公司内部系统吗？
```

### 和其他意图的区分

如果问题核心是**如何访问公司内部网络或内部系统**，归入“内网访问”。

如果问题核心是**账号能不能登录**，归入“账号与统一门户”。

例如：

> “VPN 密码错误怎么办？”
> 如果文档说明 VPN 使用统一账号，可以归入“内网访问”，但 `topic_tags` 中标注 `["VPN", "统一账号", "登录失败"]`。

如果问题问的是：

> “忘记统一账号密码怎么办？”

则归入“账号与统一门户”。

---

## 2.4 办公平台

### 覆盖范围

这一类统一覆盖 OA、钉钉、企业微信、飞书等组织内部办公平台。不要把一级意图写成“办公平台与 OA 使用”，因为 OA 只是办公平台的一部分。

包括：

* OA；
* 钉钉；
* 企业微信；
* 飞书；
* 移动办公 App；
* 工作台；
* 通讯录；
* 审批；
* 请假申请；
* 报销申请；
* 用印申请；
* 消息通知；
* 审批进度查询；
* 文档 / 网盘 / 微盘；
* 组织加入；
* 手机端绑定。

### 典型问题

```text
请假审批是在企业微信里提交还是在 OA 里提交？
移动办公平台第一次怎么登录？
怎么在办公平台里查同事联系方式？
审批提交后在哪里看进度？
报销附件怎么上传？
我登录企业微信后找不到公司组织怎么办？
```

### 和其他意图的区分

如果问题核心是**使用某个平台完成办公操作**，归入“办公平台”。

如果问题核心是**这个操作背后的制度规则**，归入“人事与员工事务”。

例如：

> “请假需要提前多久申请？” → 人事与员工事务
> “请假审批在哪里提交？” → 办公平台

---

## 2.5 兜底与转人工

### 覆盖范围

这一类不再包含工单、故障上报、权限申请等完整 IT 支持流程。它只负责测试 RAG 系统的边界能力。

包括：

* 知识库无答案；
* 文档没有覆盖；
* 需要人工确认；
* 涉及个人隐私；
* 涉及权限边界；
* 用户要求系统直接执行操作；
* 问题过于模糊；
* 需要转人工。

### 典型问题

```text
你能帮我查一下我这个月具体发了多少工资吗？
你能直接帮我重置密码吗？
能不能帮我查一下同事的手机号？
我按 VPN 手册操作后还是连不上，你能直接帮我修好吗？
公司有没有下午茶？
```

### 系统应该如何回答

系统不应该编造答案，也不应该假装自己能执行操作。它应该：

* 明确说明知识库中没有相关信息；
* 或说明自己不能直接查询/修改/审批/重置；
* 或提示用户联系 HR、行政、IT 或人工支持；
* 必要时说明需要提供更多信息。

### 和其他意图的区分

如果问题可以由知识库回答，就不要归入兜底。

例如：

> “VPN 连不上怎么办？”
> 如果 VPN 手册中有排查步骤，归入“内网访问”。

只有当问题变成：

> “我按手册做了还是不行，你能直接远程修好吗？”

才归入“兜底与转人工”。

---

# 3. 评测集数据格式

评测集建议使用 **JSONL**，一行一条样本，方便后续 Python 脚本读取、评测和统计。

推荐字段如下：

```json
{
  "id": "eval_0001",
  "query": "我刚入职，第一次登录公司系统需要先激活统一账号吗？",
  "intent": "账号与统一门户",
  "topic_tags": ["账号激活", "首次登录", "统一门户"],
  "difficulty": "simple",
  "answerable": true,
  "expected_docs": ["统一账号与登录认证说明.pdf"],
  "expected_sections": ["账号激活", "首次登录"],
  "evidence": [
    {
      "doc": "统一账号与登录认证说明.pdf",
      "section": "账号激活",
      "quote": "新员工首次访问统一门户前，需先完成统一账号激活。"
    }
  ],
  "reference_answer": "需要。新员工首次登录公司系统前，应先完成统一账号激活，然后再使用统一账号登录统一门户或其他内部系统。",
  "eval_focus": ["retrieval", "faithfulness", "procedure"],
  "expected_behavior": "answer_with_citation"
}
```

---

## 3.1 字段说明

| 字段                  | 作用                                  |
| ------------------- | ----------------------------------- |
| `id`                | 样本编号                                |
| `query`             | 用户问题                                |
| `intent`            | 一级意图                                |
| `topic_tags`        | 细粒度标签，不作为严格意图分类                     |
| `difficulty`        | 难度，取值为 `simple` / `medium` / `hard` |
| `answerable`        | 知识库是否可以回答                           |
| `expected_docs`     | 正确答案应命中的文档                          |
| `expected_sections` | 正确答案应命中的章节，可选                       |
| `evidence`          | 标准证据片段                              |
| `reference_answer`  | 标准答案                                |
| `eval_focus`        | 评测重点                                |
| `expected_behavior` | 期望行为，例如回答、拒答、转人工                    |

---

## 3.2 为什么不绑定 chunk_id

评测集不建议绑定固定 `chunk_id`，因为后续你可能会调整 chunk size、chunk overlap 或切分策略。如果评测集绑定 chunk_id，一改切分方法，评测集就失效。

更稳定的做法是绑定：

```text
文档名 + 章节名 + evidence 原文片段
```

这样即使重新切 chunk，也可以判断 Top-K 检索结果中是否包含对应证据。

---

# 4. 难度划分

建议使用三档难度。

| 难度       | 定义                    | 示例                           |
| -------- | --------------------- | ---------------------------- |
| `simple` | 单文档、单片段即可回答           | “统一账号第一次怎么激活？”               |
| `medium` | 单文档多步骤，或需要结合条件判断      | “VPN 登录成功但 OA 打不开怎么办？”       |
| `hard`   | 多文档、多条件、不可回答、隐私边界或转人工 | “OA 能登录但 VPN 登录失败，是不是账号不一样？” |

难度不是为了让问题看起来复杂，而是为了后续统计系统在不同类型问题上的表现。

---

# 5. eval_focus 标签

`eval_focus` 用于标注这条样本主要测试什么能力。推荐使用固定标签，方便统计。

```text
retrieval              检索是否命中正确证据
faithfulness           回答是否忠实于文档
procedure              是否能组织流程步骤
condition              是否能处理条件判断
multi_doc_retrieval    是否需要跨文档检索
troubleshooting        是否涉及排查类问题
citation               引用是否准确
unanswerable           是否识别不可回答
privacy_boundary       是否识别隐私/权限边界
fallback               是否能兜底
redirect_to_human      是否能转人工
```

---

# 6. 评测集应该测什么指标

评测应该分为两层：

```text
检索评测
生成评测
```

不要只测最终答案，因为 RAG 出错可能来自两个阶段：

1. 没检索到正确文档；
2. 检索到了，但回答时没用好、漏答或幻觉。

---

## 6.1 检索评测指标

主要评估系统是否找到了正确依据。

建议指标：

| 指标         | 含义                                       |
| ---------- | ---------------------------------------- |
| `Hit@K`    | Top-K 检索结果中是否命中 expected_docs 或 evidence |
| `Recall@K` | 应该命中的证据有多少被召回                            |
| `MRR`      | 正确证据在排序中是否靠前                             |
| `NDCG@K`   | 多个相关结果的排序质量                              |

第一版可以先做最简单的：

```text
Hit@3
Hit@5
MRR
```

其中：

* `Hit@3`：看前 3 个检索结果中是否有正确证据；
* `Hit@5`：看前 5 个检索结果中是否有正确证据；
* `MRR`：看正确结果排得靠不靠前。

---

## 6.2 生成评测指标

主要评估最终回答是否可用。

建议人工或 LLM-as-judge 从以下维度打分：

| 指标        | 含义           |
| --------- | ------------ |
| 正确性       | 是否回答了用户问题    |
| 完整性       | 是否遗漏关键步骤或条件  |
| 忠实性       | 是否严格基于检索到的文档 |
| 可执行性      | 用户是否能照着回答操作  |
| 引用准确性     | 引用文档是否真的支持回答 |
| 拒答/转人工正确性 | 不可回答时是否正确兜底  |

可以采用 0/1 或 0/2 分制。

例如：

```text
0 = 错误 / 不满足
1 = 部分满足
2 = 完全满足
```

---

## 6.3 系统对比指标

为了体现项目改进效果，可以做消融实验。

建议优先比较：

| 对比实验                     | 目的                       |
| ------------------------ | ------------------------ |
| 向量检索 vs 混合检索             | 看关键词检索是否提升系统名、流程名、术语检索效果 |
| 不同 chunk size            | 看文档切分对检索效果的影响            |
| 无 reranker vs 有 reranker | 看重排序是否提升 Top-K 质量        |
| 无引用 vs 有引用               | 看可溯源回答是否更可靠              |
| 无兜底规则 vs 有兜底规则           | 看不可回答问题是否更稳              |

第一版最推荐做：

```text
向量检索 vs 混合检索
不同 chunk size
有无 reranker
```

---

# 7. 为什么这样设计评测集

## 7.1 一级意图不照搬文档目录

文档目录通常是作者组织内容的方式，不一定等于用户提问方式。比如员工手册里可能有“聘用、入职、转正、调动、离职、退休、考勤、休假、薪酬、绩效”等章节，但用户问题未必会按这些标题提问。

所以我们不把一级意图设计成“入职流程、考勤、薪酬、绩效”等过细分类，而是抽象为：

```text
人事与员工事务
```

这样更符合用户问题分类，也更方便后续扩展。

---

## 7.2 不设置严格二级意图

第一版不做二级意图，是因为你的项目重点不是意图识别，而是 RAG 问答。

如果强行设置二级意图，会带来两个问题：

1. 二级意图容易变成具体问题模板；
2. 系统评测会从“RAG 检索生成”变成“意图分类 + 检索 + 生成”，复杂度不必要地上升。

因此使用：

```text
一级意图 + topic_tags
```

一级意图用于统计分析，`topic_tags` 用于保留细粒度信息。

---

## 7.3 不直接依赖真实企业工单

真实企业内部工单和内部知识库通常不公开。项目可以参考公开的高校、机构、信息化中心文档，以及企业办公平台使用材料，构造贴近中国企业场景的模拟知识库和评测集。

关键要求是：

> 每条评测问题都要有文档依据，而不是凭空生成。

因此评测样本必须包含：

```text
expected_docs
expected_sections
evidence
reference_answer
```

---

## 7.4 同时测可回答和不可回答

真实 RAG 系统不仅要会回答，还要知道什么时候不能回答。

所以评测集中要包含少量：

* 知识库无答案；
* 需要人工确认；
* 涉及个人隐私；
* 要求系统直接执行操作；
* 问题过于模糊。

这些样本归入：

```text
兜底与转人工
```

它们用于测试系统是否会幻觉、越权或乱答。

---

## 7.5 评测集要适应 chunk 策略变化

你的 chunk 可能会修改，因此评测集不能依赖固定 chunk。使用文档名、章节名和 evidence 原文作为稳定标注，可以保证后续切分策略变化时评测集仍然可用。

这对后续实验很重要，因为你可能要比较：

```text
不同 chunk size
不同 overlap
递归切分 vs 结构化切分
向量检索 vs 混合检索
有无 reranker
```

---

# 8. 第一版评测集规模建议

第一版建议先做 30 条，跑通评测链路。

推荐分布：

| 一级意图    | 数量 |
| ------- | -: |
| 人事与员工事务 |  8 |
| 账号与统一门户 |  6 |
| 内网访问    |  5 |
| 办公平台    |  7 |
| 兜底与转人工  |  4 |
| 合计      | 30 |

难度分布：

| 难度     | 数量 |
| ------ | -: |
| simple | 12 |
| medium | 12 |
| hard   |  6 |

后续系统稳定后，可以扩展到 100 条左右。第一版不建议一开始做太大，先保证每条数据质量和评测代码可运行。
